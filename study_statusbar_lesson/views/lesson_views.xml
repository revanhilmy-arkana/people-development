<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_statusbar_lesson_form" model="ir.ui.view">
        <field name="name">study.statusbar.lesson.form</field>
        <field name="model">study.statusbar.lesson</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_in_progress" string="In Progress" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_done" string="Mark As Done" type="object" class="oe_highlight" invisible="state != 'in_progress'"/>
                    <button name="action_cancel" string="Cancel" type="object" invisible="state in ('done', 'cancel')"/>
                    <button name="action_draft" string="Reset to Draft" type="object" invisible="state != 'cancel'"/>
                    
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="description"/>
                        </group>
                        <group>
                            <!-- Conditional invisible field -->
                            <field name="hide_me" invisible="state != 'done'"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_statusbar_lesson_tree" model="ir.ui.view">
        <field name="name">study.statusbar.lesson.tree</field>
        <field name="model">study.statusbar.lesson</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-warning="state == 'in_progress'" decoration-success="state == 'done'" decoration-danger="state == 'cancel'"/>
            </list>
        </field>
    </record>

    <record id="action_statusbar_lesson" model="ir.actions.act_window">
        <field name="name">Statusbar Lessons</field>
        <field name="res_model">study.statusbar.lesson</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
